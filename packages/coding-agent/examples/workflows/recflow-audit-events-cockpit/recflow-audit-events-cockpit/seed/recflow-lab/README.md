# recflow-lab

Local OMH validation target: a tiny recursive workflow runner built through a
multi-agent OMH workflow. Plans are JSON trees with `task`, `sequence`,
`parallel`, `branch`, and bounded `loop` nodes. Run `bun test` to validate the
engine and CLI behavior.
