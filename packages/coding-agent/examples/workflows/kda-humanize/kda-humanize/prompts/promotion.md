Decide whether the recorded KDA evidence supports promotion.

{{evidence}}

Use `promote` only when the evidence proves the task contract, validation
requirement, and rollback criteria are satisfied. Use `reject` when the evidence
is incomplete, stale, negative, or only shows a demo/smoke result for a task that
requires real project evidence.

Mention the strongest evidence and any residual risk, then end with exactly one
final token: `reject` or `promote`.
